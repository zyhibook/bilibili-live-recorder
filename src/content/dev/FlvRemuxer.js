import { mergeBuffer } from '../../share';

export default class FlvRemuxer {
    constructor(bg) {
        this.bg = bg;
        this.data = new Uint8Array(10);
    }

    load(buf) {
        //
    }

    stop() {
        //
    }

    download() {
        //
    }
}
